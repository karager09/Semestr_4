 

#include <stdio.h>

long long sum3( unsigned int a,unsigned int b,unsigned int c )
{

		return a+b+c;
}

long long sum3as( unsigned int a, unsigned int b,unsigned int c);


void main( void )
{
 //int i;
	int a,b,c;
 //for( i = 0; i <= 100; i++ )
	for(a=5; a<10; a++)
		for(b=5; b<10; b++)
			for(c= 5; c<10; c++)
   			printf( "Suma %d + %d + %d jest równa: %ld,  %ld\n", a,b,c, sum3(a,b,c), sum3as(a,b,c) );
}